<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Post-Shipment Inspection Report</title>
    <style>
      body {
        font-family: 'Segoe UI', sans-serif;
        padding: 30px;
        background-color: #f9fbfd;
        color: #333;
      }

      .header {
        background-color: #283E55;
        color: white;
        padding: 12px 20px;
        border-radius: 8px;
        margin-bottom: 25px;
      }

      .section {
        margin-bottom: 30px;
      }

      .section h3 {
        background-color: #e4ebf5;
        padding: 10px;
        border-radius: 6px;
        font-size: 18px;
        margin-bottom: 15px;
      }

      .details-table {
        width: 100%;
        border-collapse: collapse;
      }

      .details-table td {
        padding: 10px;
        border-bottom: 1px solid #ccc;
      }

      .details-table td:first-child {
        font-weight: bold;
        width: 250px;
      }

      .media-block {
        text-align: center;
        margin-top: 20px;
      }

      .media-block img {
        max-width: 500px;
        height: auto;
        border: 1px solid #ccc;
        border-radius: 10px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.2);
      }

      .notes {
        background-color: #e8f1fc;
        padding: 12px;
        border-radius: 6px;
        white-space: pre-wrap;
        line-height: 1.6;
      }

      .footer {
        font-size: 12px;
        text-align: center;
        margin-top: 40px;
        color: #777;
      }
    </style>
  </head>

  <body>
    <div class="header">
      <h2>Post-Shipment Inspection Report</h2>
      <p>Contract ID: <%= report.contract_id %></p>
    </div>

    <div class="section">
      <h3>Inspection Summary</h3>
      <table class="details-table">
        <tr><td>All Items Present:</td><td><%= report.items_present ? 'Yes' : 'No' %></td></tr>
        <tr><td>Any Items Damaged:</td><td><%= report.items_damaged ? 'Yes' : 'No' %></td></tr>
        <tr><td>Customer Rating:</td><td><%= report.rating || 'Not provided' %></td></tr>
      </table>
    </div>

    <div class="section">
      <h3>Goods Arrival Photo</h3>
      <div class="media-block">
        <% if (report.arrival_photo_url) { %>
          <img src="<%= report.arrival_photo_url %>" alt="Goods Arrival Photo" />
        <% } else { %>
          <p><em>No photo available</em></p>
        <% } %>
      </div>
    </div>

    <% if (report.damage_photo_url) { %>
      <div class="section">
        <h3>Damage Photo</h3>
        <div class="media-block">
          <img src="<%= report.damage_photo_url %>" alt="Damage Photo" />
        </div>
      </div>
    <% } %>

    <div class="section">
      <h3>Final Notes</h3>
      <div class="notes">
        <%= report.notes || 'No additional comments.' %>
      </div>
    </div>

    <div class="footer">
      Generated by SHAREMART | Post-Shipment 
    </div>
  </body>
</html>